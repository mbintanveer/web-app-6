import { DoctorfilterPipe } from './doctorfilter.pipe';

describe('DoctorfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DoctorfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
